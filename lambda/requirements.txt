# boto3 is preinstalled in the AWS Lambda Python runtime.
# Bundle with Linux x86_64 + Python 3.12 wheels (Makefile / terraform null_resource).
PyJWT==2.9.0
cryptography>=43,<50
youtube-transcript-api>=1.2.4,<1.3
